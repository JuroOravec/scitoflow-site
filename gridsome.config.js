'use strict';

// This is a mandatory entrypoint for gridsome config.
// The rest of the config is defined in a TypeScript file and compiled
// before we start Gridsome.
//
// See `src/gridsome.config.ts`
const gridsomeConfig = require('./dist/gridsome/gridsome.config');

module.exports = gridsomeConfig;
